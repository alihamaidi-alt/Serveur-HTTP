Une tentative de créer un serveur HTTP simple en Python avec plusieurs page
Le code n'est pas encore parfait, car quand je vais sur /apropos ça fonctionne normal mais dès que j'ajoute quelque chose dans l'URL,(par exemple ?lang=fr)
le programme ne reconnaît plus la page et affiche directement la 404. 
Mon prochain objectif c'est d'apprendr à utiliser une bibliothèque comme Flask ou FastAPIplutot que que http.server pour des vrais projets 
